services:
  autopvs1-link:
    ports: !reset []
    expose:
      - "8000"
    networks:
      - npm
    labels:
      - "npm.proxy=true"

networks:
  npm:
    external: true
    name: nginx-proxy-manager_default
